# אפיון טכני - אפליקציית קטלוג יצירות אמנות

> גרסה מעודכנת לאחר סשן אפיון וחשיבה (מבוססת על קובץ אקסל אמיתי מהשטח).
> המסמך המקורי שצורף נמצא כהיסטוריה בשיחה; זהו המסמך המחייב לתכנון וליישום.

## תיאור כללי

בניית אפליקציית PWA (Progressive Web App) לניהול קטלוג יצירות אמנות (ציורים)
פנימי לגלריה/חנות. האפליקציה תשמש 2-4 אנשי צוות, תעבוד גם אופליין וגם
אונליין, ותסתמך על Google Sheets כמסד נתונים משותף בענן (חינמי, ללא מנוי,
ללא שרת עצמאי לתחזוקה) ועל Google Drive לאחסון תמונות.

## מטרת המערכת

לאפשר לצוות הגלריה לחפש, לצפות, להוסיף ולערוך פריטי מלאי (עד ~200 פריטים
כיום, עם תמיכה בגדילה לעד 1000) במהירות - מכל מכשיר, עם או בלי חיבור
לאינטרנט, כאשר עדכון של אחד מהצוות מסתנכרן לכולם.

**מטרה חשובה נוספת:** בנתוני האמת הקיימים היום, חלק גדול מהפריטים חסרים
מספר סידורי ו/או מחיר. האפליקציה נועדה גם לאפשר **השלמה הדרגתית** של נתונים
חסרים על פריטים קיימים - לא רק הזנת פריטים חדשים ומלאים.

---

## ארכיטקטורה

```
דפדפן (PWA)  <-- HTTPS -->  Google Apps Script (Web App)  <-->  Google Sheets
     |                                                      \--> Google Drive (תמונות)
     v
  IndexedDB (Dexie.js) - קאש מקומי + תור סנכרון
```

- **Frontend**: PWA סטטי טהור - React + Vite, `manifest.json` + Service
  Worker (התקנה למסך הבית, קאשינג נכסי האפליקציה עצמה).
- **Backend**: **Google Apps Script** אחד, פרוס כ-Web App, שמחזיק את
  ההרשאה ל-Sheet ול-Drive וחושף API בפני ה-PWA. זו הסיבה: אי אפשר להטמיע
  מפתח Service Account בקוד צד-לקוח סטטי בלי לחשוף אותו לכל מי שיפתח
  DevTools - Apps Script פותר את זה בלי צורך בשרת אמיתי לתחזק, וללא עלות.
  ה-Apps Script גם קובע את `last_modified_at` בפועל (חותמת זמן צד-שרת),
  כדי ש-Last-Write-Wins יהיה אמין ולא תלוי בשעון מכשיר.
- **מסד נתונים**: Google Sheets, טאב אחד עם כל השורות + טאב `Config` נפרד
  לרשימות הערכים הניתנות להרחבה (מיקום/סוג/סטטוס).
- **אחסון תמונות**: Google Drive (תיקייה ייעודית), קבצים משותפים
  "לכל מי שיש לינק", ה-`image_url` מצביע לקובץ.
- **Hosting**: **GitHub Pages** - ה-Frontend סטטי לגמרי, אין תלות בפלטפורמת
  אחסון עם פונקציות שרת (זה כבר לא נדרש ברגע שהבחירה היא Apps Script).
- **זהות משתמש**: אין login אמיתי. בפתיחה ראשונה במכשיר, בחירת שם מרשימה
  סגורה של 2-4 אנשי הצוות, נשמר ב-`localStorage`. משמש למילוי
  `last_modified_by`.

---

## מבנה הנתונים

כל פריט (Item) מכיל:

| שדה | תיאור | הערות |
|---|---|---|
| `row_id` | מזהה פנימי | **המזהה הראשי (Primary Key) האמיתי של השורה, לצורך סנכרון בלבד.** לא מוצג למשתמש. נוצר **בצד הלקוח** (UUID/קוד קצר) ברגע יצירת השורה - תמיד קיים, לעולם לא ריק. `upsert`/`softDelete` מזהים שורה לפי `row_id`, לא לפי `serial_number` |
| `serial_number` | מספר סידורי (תג פיזי) | שדה **עסקי בלבד** (המספר שרואים על התג הפיזי). **אופציונלי (nullable)** - יכול להישאר ריק ולהתמלא בכל שלב, בלי שום השפעה על יכולת הסנכרון (ראו `row_id`) |
| `sku` (מק"ט) | קוד מוצר/דגם | **יכול לחזור** על כמה שורות (למשל אותה יצירה בכמה גרסאות) |
| `name` | שם היצירה | טקסט חופשי |
| `size` (גודל) | מידות מדויקות | טקסט חופשי (למשל `91X132`), לא קטגוריה קבועה |
| `type` (סוג) | מקורי / מיקס מדיה | רשימת ערכים **ניתנת להרחבה** מתוך האפליקציה (ראו סעיף Config) - לא קבועה בקוד |
| `location` (מיקום) | מיקום פיזי (למשל אביגדור, גלריה, חיים) | רשימת ערכים **ניתנת להרחבה** מתוך האפליקציה |
| `physical_status` (סטטוס) | מגולגל / מתוח | רשימת ערכים **ניתנת להרחבה** מתוך האפליקציה |
| `availability_status` | זמין / נמכר | שדה **עסקי**, לא מחיקה. ברירת מחדל: זמין. פריט "נמכר" **נשאר גלוי** בקטלוג עם סימון ויזואלי, וניתן לסנן לפי הסטטוס (למשל "הצג רק זמינים") |
| `price` | מחיר | מספרי, **אופציונלי (nullable)** - ניתן להשלים בדיעבד |
| `notes` (הערות) | הערות | טקסט חופשי פנימי גרידא - **ללא כל לוגיקה נוספת** (לא סטטוס, לא דגל) |
| `image_url` | קישור לתמונה ב-Drive | מתמלא אוטומטית לאחר העלאה דרך האפליקציה |
| `is_deleted` | דגל מחיקה רכה | **שונה מ-`availability_status`!** משמש רק לתיקון טעויות/כפילויות; פריט כזה **מוסתר לחלוטין** מכל תצוגה, לתמיד |
| `last_modified_by` | מי שינה אחרון | נבחר מרשימת השמות הסגורה |
| `last_modified_at` | מתי שונה אחרון | timestamp - **נקבע ע"י ה-Apps Script** בזמן הכתיבה בפועל |

### טאב Config (רשימות ערכים ניתנות להרחבה)

טאב נפרד ב-Sheet, עם עמודות כמו `list_name` (type / location /
physical_status) ו-`value`. אלה **לא רשימות סגורות לצמיתות** - כל אחת
מהן היא נקודת התחלה שניתן להוסיף לה ערכים חדשים בכל עת ישירות מתוך
האפליקציה (למשל כשנוסף מיקום פיזי חדש שלא היה קיים קודם), בלי צורך
בשינוי קוד. הוספת ערך אפשרית גם inline מתוך טופס הוספה/עריכה של פריט
("+ הוסף ערך חדש"), וגם ממסך ניהול רשימות ייעודי; הערך נכתב חזרה ל-Config
דרך Apps Script וזמין מיד לכל הצוות ברענון הבא. מחיקת ערך קיים מרשימה
אינה נתמכת (כדי לא לשבור פריטים קיימים שכבר משתמשים בו) - רק הוספה.

**ערכי ברירת מחדל (מיובאים מהאקסל הקיים):**

| רשימה | ערכים התחלתיים |
|---|---|
| `location` (מיקום) | אביגדור, גלריה, חיים |
| `type` (סוג) | מקורי, מיקס מדיה |
| `physical_status` (סטטוס) | מגולגל, מתוח |

---

## פונקציונליות נדרשת

### 1. חיפוש וסינון
- חיפוש לפי מק"ט (טקסטואלי, מחזיר רשימת תוצאות)
- חיפוש לפי מספר סידורי (מדויק, מחזיר פריט בודד)
- סינון: **"פריטים ללא מחיר"**, **"פריטים ללא מק"ט"** - לצורך השלמת נתונים בשיטתיות (ראו גם סעיף מיגרציה - לאחר המיגרציה לכל הפריטים יהיה מספר סידורי, כך שהסינון "ללא מספר סידורי" רלוונטי בעיקר לפריטים חדשים שנוספו בעתיד ללא השלמה מיידית)
- סינון לפי `availability_status` (הצג זמינים בלבד / הצג הכל / הצג נמכרים בלבד)
- (אופציונלי לעתיד: סינון לפי טווח מחירים, סוג, מיקום)

### 2. ניהול פריטים (CRUD)
- הוספת פריט חדש (השדות `serial_number`/`price` יכולים להישאר ריקים בהוספה)
- עריכת פריט קיים - כולל **השלמת שדות חסרים** על פריט קיים (מספר סידורי, מחיר)
- שינוי `availability_status` ל"נמכר" - עריכה רגילה, ללא חלון אישור
- מחיקת פריט (תיקון טעות/כפילות) - **עם חלון אישור** ("האם אתה בטוח?"), soft-delete (`is_deleted=true`), מוסתר תמיד
- כל המשתמשים ברמת הרשאה שווה (אין היררכיה)

### 3. תמונות
- העלאה **ישירה מתוך האפליקציה** (מצלמה/גלריה של המכשיר)
- כיווץ/שינוי גודל בצד לקוח לפני העלאה (JPEG, רוחב מקסימלי סביר, למשל 1600px) - כדי לשמור על מהירות ונפח סביר
- ה-PWA שולחת את התמונה (base64) ל-Apps Script; הוא שומר ל-Drive, מגדיר שיתוף "לכל מי שיש לינק", ומחזיר URL שנשמר ב-`image_url`
- **אופליין**: אם אין חיבור, התמונה נשמרת מקומית (IndexedDB) כ"ממתינה להעלאה" ומועלית בפועל כשיש אינטרנט, כחלק מתור הסנכרון

### 4. סנכרון עם Google Sheets
- Google Sheets משמש כ"מקור אמת" מרכזי ומשותף, מאחורי Apps Script
- בגלל היקף נתונים קטן (200-1000 שורות), כל רענון מושך את **כל** מסד הנתונים (אין צורך במנגנון "מה השתנה מאז X")
- רענון אוטומטי כל 30-60 שניות כשיש אינטרנט, בנוסף לכפתור רענון ידני
- סדר פעולות בסנכרון: **קודם דוחפים** שינויים ממתינים מהתור המקומי (עם retry), **ואז מושכים** סנפשוט טרי - כדי שהשינוי של המשתמש "ינצח" עם ה-timestamp הסופי מהשרת ולא יידרס ע"י פולינג מקבילי

### 5. עבודה אופליין
- שמירת עותק מקומי (IndexedDB דרך Dexie.js) של כל הנתונים במכשיר
- כשאין אינטרנט: צפייה, חיפוש, הוספה ועריכה - הכל עובד; שינויים נשמרים בתור מקומי ("ממתין לסנכרון")
- כשמתחדש חיבור: סנכרון אוטומטי (ראו סעיף 4)

### 6. פתרון התנגשויות (Conflict Resolution)
- מדיניות: **"האחרון מנצח"** (Last Write Wins), לפי timestamp שנקבע **בצד השרת** (Apps Script) בזמן הכתיבה - לא שעון המכשיר
- אין מנגנון מיזוג מורכב, ואין התראה למשתמש על דריסה (הוחלט להשאיר פשוט ב-MVP)

### 7. היסטוריה בסיסית
- כל שורה שומרת `last_modified_by` ו-`last_modified_at`
- Google Sheets מספק גם Version History מובנה (גיבוי טבעי, שחזור ידני דרך ממשק Sheets)

### 8. רב-לשוניות (עברית / אנגלית / הולנדית)
- **קהל יעד**: צוות פנימי בלבד (לא לקוחות) - כפתור דגל בכותרת למעבר שפה.
- **כלל ההפרדה (חשוב)**: מתורגמות **תוויות ומחרוזות UI** בלבד (כפתורים, שמות שדות,
  הודעות, טקסטים קבועים). **תוכן** - שם היצירה, הערות, וגם ערכי הרשימות הניתנות-להרחבה
  (`location`/`type`/`physical_status`, למשל "אביגדור"/"מיקס מדיה"/"ממוסגר") - **נשאר
  תמיד כפי שהוזן** (בעברית בד"כ), ללא תרגום, כי אלה תוכן שהצוות מזין ולא ניתן לתרגם
  מראש. **חריג**: `availability_status` ("זמין"/"נמכר") הוא שדה מערכת קבוע (לא Config
  שהצוות עורך) ולכן **כן** מתורגם כמו כל מחרוזת UI אחרת.
- **כיוון (RTL/LTR)**: מבנה הדף נשאר RTL תמיד, גם כשבוחרים אנגלית/הולנדית - מתחלף רק
  הטקסט, לא הפריסה (mirror מלא נשקל כלא מוצדק לשימוש פנימי מזדמן).
- **מטבע**: המחיר מוצג תמיד ב-₪, ללא המרה למטבע זר.
- **שמירת בחירת שפה**: per-device ב-localStorage (כמו בחירת שם המשתמש).
- **רשימת השמות בבחירת הזהות** (מי אתה) - נשארת תמיד בעברית, בלי קשר לשפת התצוגה.
- **מימוש**: טבלת מחרוזות פשוטה (מפתח → תרגום) לכל שפה, נטענת בצד לקוח; אין צורך
  בספריית i18n חיצונית בהיקף הזה.

---

## Backend API (Apps Script Web App)

Endpoints מוצעים (JSON מעל HTTP):

| Endpoint | תיאור |
|---|---|
| `getAll` | מחזיר את כל השורות הפעילות (`is_deleted != true`) + טאב ה-Config |
| `upsert` | יוצר/מעדכן שורה **לפי `row_id`** (אם קיימת שורה עם אותו `row_id` - מעדכן; אחרת יוצר שורה חדשה); קובע `last_modified_at`/`last_modified_by` בצד שרת |
| `softDelete` | מסמן `is_deleted=true` על שורה קיימת, לפי `row_id` |
| `uploadImage` | מקבל תמונה (base64) + `row_id`, שומר ל-Drive, מחזיר URL, ומעדכן `image_url` בשורה |
| `addConfigOption` | מוסיף ערך חדש לרשימת ערכים (type/location/physical_status) בטאב Config; **בודק קודם אם הערך כבר קיים** (case-insensitive) כדי למנוע כפילויות כשכמה מכשירים מוסיפים באותו זמן |

### דרישות טכניות קריטיות למימוש ה-API

נמצאו בסקירת האפיון לפני תחילת הפיתוח - חובה לממש כך, אחרת האינטגרציה תיכשל בפועל:

- **CORS/Preflight**: קריאת `fetch()` מהדפדפן עם גוף מסוג `application/json` מפעילה בקשת
  preflight (`OPTIONS`) - ו-**Apps Script לא תומך ב-OPTIONS**, כך שכל קריאה כזו תיכשל
  עם שגיאת CORS. הפתרון: ה-Frontend שולח את גוף הבקשה כ-`Content-Type: text/plain`
  (סוג "safelisted" שלא מפעיל preflight), וה-Apps Script קורא ומפרש אותו בעצמו:
  `JSON.parse(e.postData.contents)`.
- **נעילה (Locking)**: כל endpoint שכותב ל-Sheet (`upsert`/`softDelete`/`addConfigOption`)
  חייב לעטוף את הקריאה ב-`LockService.getScriptLock()`, כדי למנוע מרוץ תנאים כששני
  אנשי צוות כותבים במקביל (שורה כפולה, נתונים חלקיים).
- **הגנה קלה על ה-URL**: מכיוון שאין login, ה-Web App URL עצמו הוא בפועל ההגנה
  היחידה על הגישה, והוא מוטמע בקוד ה-Frontend (גלוי למי שיפתח DevTools). כשכבת
  הגנה נוספת, כל בקשה כוללת "סוד משותף" קבוע (מחרוזת קבועה מראש, לא סוד קריפטוגרפי
  אמיתי) שנשלח בגוף הבקשה ונבדק ב-Apps Script לפני כל פעולה; בקשה בלי הסוד הנכון
  נדחית. זה לא מחליף אבטחה אמיתית, אבל מונע גישה מקרית/סריקה אוטומטית של כתובת URL.

---

## שלבי פיתוח מוצעים (MVP → Full)

**שלב 1 - MVP:**
- Apps Script בסיסי: `getAll` + `upsert` + `softDelete`
- טעינת נתונים, תצוגה בטבלה/כרטיסים, חיפוש לפי מק"ט ומספר סידורי
- סינון "חסר מספר סידורי" / "חסר מחיר" / לפי availability_status
- הוספה/עריכה/מחיקה (עם אישור) מהאפליקציה, כתיבה חזרה ל-Sheets
- PWA בסיסי (manifest + service worker), התקנה למסך הבית
- עבודה אופליין עם תור סנכרון (IndexedDB/Dexie)
- בחירת שם משתמש מרשימה סגורה
- הוספת ערך חדש inline לרשימות type/location/physical_status (מתוך טופס פריט) - חלק מה-MVP, לא תוספת עתידית
- **מנגנון רב-לשוניות** (טבלת מחרוזות + כפתור החלפת שפה) נבנה כבר בשלב זה, כדי שכל
  מסך נכתב מההתחלה עם "מפתח טקסט" ולא טקסט קבוע (עוטפים אחר כך זה עבודה כפולה).
  התרגום בפועל לאנגלית/הולנדית (מילוי הערכים בטבלה) יכול להיכנס בשלב מאוחר יותר -
  המנגנון מוכן, גם אם התוכן חסר בהתחלה.

**שלב 2:**
- מילוי בפועל של תרגומי האנגלית וההולנדית בטבלת המחרוזות
- העלאת תמונות (`uploadImage`, כיווץ בצד לקוח, אופליין עם תור)
- מסך ניהול רשימות ייעודי (תצוגה מרוכזת של כל הערכים הקיימים לכל רשימה)

**שלב 3:**
- שיפורי UI/UX, סינון לפי טווח מחירים/סוג/מיקום
- שיפורי ביצועים אם הכמות מתקרבת ל-1000 פריטים

---

## סיכום החלטות שהוכרעו בסשן האפיון

| נושא | החלטה |
|---|---|
| חיבור ל-Google Sheets | Google Apps Script Web App כבק-אנד (לא Service Account בצד לקוח, לא OAuth פר-משתמש) |
| זהות משתמש | בחירת שם מרשימה סגורה, נשמר ב-localStorage |
| יצירת פריט אופליין | מותר; מספר סידורי = קוד קצר שנוצר בצד לקוח |
| מחיקה מול "נמכר" | שני מושגים נפרדים: `is_deleted` (מוסתר, לתיקון טעויות) מול `availability_status` (גלוי, ניתן לסינון) |
| רשימות type/location/physical_status | לא קבועות בקוד - ניתנות **להרחבה** מתוך האפליקציה בכל עת (טאב Config נפרד ב-Sheet); ערכי התחלה מיובאים מהאקסל |
| העלאת תמונות | ישירה מתוך האפליקציה (base64 → Apps Script → Drive) |
| Hosting | GitHub Pages |
| timestamp ל-LWW | נקבע בצד ה-Apps Script, לא שעון הלקוח |
| חשבון Google | חשבון העסק/הגלריה הקיים - המשתמש יתחבר אליו בעצמו בשלב ההגדרה בפועל |
| Primary Key לסנכרון | `row_id` פנימי חדש (לא מוצג), נפרד מ-`serial_number` העסקי-הריק-אפשרי |
| CORS מול Apps Script | גוף בקשה כ-`text/plain` (לא `application/json`) כדי לעקוף preflight |
| כתיבות מקבילות | `LockService.getScriptLock()` בכל endpoint שכותב ל-Sheet |
| הגנת URL | שכבת "סוד משותף" קלה נבדקת בכל בקשה ב-Apps Script |
| רב-לשוניות | עברית/אנגלית/הולנדית, לצוות פנימי בלבד; מתורגם UI בלבד, תוכן (כולל ערכי Config) נשאר כפי שהוזן; מבנה RTL קבוע; מנגנון נבנה בשלב 1, תרגום בפועל בשלב 2 |

---

## תוכנית מיגרציה מהאקסל הקיים

שלב הקמה חד-פעמי, לפני ההשקה, שמייבא את הנתונים הקיימים ל-Sheet החדש:

0. **`row_id`**: כל שורה מקבלת `row_id` פנימי ייחודי במיגרציה (בנוסף ל-`serial_number`,
   ראו מבנה הנתונים) - זהו המזהה האמיתי לצורך סנכרון מרגע ההשקה.
1. **תמונות**: חילוץ כל התמונות המוטמעות מקובץ האקסל, העלאה ל-Google Drive
   (תחת חשבון העסק), וקישור כל תמונה לשורה המתאימה שלה דרך `image_url`.
2. **פריטים ללא מספר סידורי**: מקבלים **קוד קצר אוטומטי כבר בזמן המיגרציה**
   - כל שורה יוצאת מהמיגרציה עם `serial_number` ייחודי ומלא, ואינה נשארת
     ריקה בהמתנה להשלמה ידנית.
3. **פריטים ללא מק"ט**: נשארים עם `sku` ריק. **אין** ניסיון שיוך אוטומטי לפי
   דמיון בשם - אלה יעברו ידנית לאחר המיגרציה. השדה הריק מזוהה ומנוטר דרך
   סינון "פריטים ללא מק\"ט" (ראו סעיף חיפוש וסינון).
4. **מחיר**: נשאר ריק בכל השורות במיגרציה (לא קיים בקובץ המקור כלל), יושלם
   בהדרגה דרך האפליקציה.

---

## הערות תפעוליות (מסקירת האפיון)

נקודות מודעות שאינן חוסמות התחלת פיתוח, אבל כדאי לדעת עליהן מראש:

- **פריסה מחדש אחרי כל שינוי קוד**: עריכת קוד ה-Apps Script לא נכנסת לתוקף
  ב-URL הקיים אוטומטית - צריך "Manage deployments" → עדכון ה-deployment הקיים
  (לא ליצור deployment חדש בכל פעם, אחרת ה-URL משתנה).
- **Service Worker דרך ספרייה מוכרת**: מומלץ להשתמש ב-`vite-plugin-pwa`
  (מבוסס Workbox) במקום Service Worker כתוב ידנית, כדי שעדכוני גרסה
  (deploy חדש ל-GitHub Pages) יתעדכנו נכון אצל המשתמשים (עם הודעת "גרסה חדשה
  זמינה, רענן") ולא ייתקעו על גרסה ישנה בקאש.
- **iOS Safari**: אפליקציית PWA שנוספה למסך הבית ב-iPhone עלולה לאבד נתוני
  אחסון מקומי (IndexedDB) אחרי תקופה ארוכה של אי-שימוש. בשימוש יומיומי של
  הצוות זה סיכון נמוך, אך שווה להיות מודעים אליו.
- **מדיניות שיתוף ב-Drive**: אם חשבון העסק הוא Google Workspace (לא Gmail
  רגיל), ייתכן שמדיניות הארגון חוסמת שיתוף קבצים "לכל מי שיש לינק" - יש
  לוודא זאת כשמגדירים את תיקיית Drive בפועל.

---

## נקודות פתוחות שנותרו לתחילת הפיתוח

אין נקודות פתוחות מהותיות שנותרו - האפיון סגור למעבר לתכנון היישום.
