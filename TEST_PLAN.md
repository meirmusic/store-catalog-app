# תוכנית בדיקות (QA Test Plan) - קטלוג הגלריה

> מסמך זה הוא הבסיס המחייב לבניית חבילת הבדיקות האוטומטית. הוא נשען על
> `SPEC.md` (מסמך האפיון המחייב) ועל בדיקה ישירה של הקוד הקיים. כל שינוי
> עתידי ב-SPEC.md או בקוד אמור להתעדכן גם כאן - זהו מסמך חי, לא תמונת מצב
> חד-פעמית.

## 1. מטרה ומתודולוגיה

המטרה: לתפוס רגרסיות ופערים **לפני** שהצוות בשטח נתקל בהם, ולתעד באופן
שיטתי (לא אד-הוק) מה בדיוק כן מאומת ומה לא.

### רמות בדיקה (Test Levels)

| רמה | מה זה בודק | כלי |
|---|---|---|
| **Unit** | לוגיקה טהורה, בבידוד מלא - גם בצד הלקוח וגם **בצד ה-Apps Script** (ר' סעיף 6) | Node.js פשוט, בלי דפדפן |
| **Integration/Contract** | האפליקציה מול **תשובות מדומות** של ה-Apps Script (הצלחה/כישלון/שגיאה) - בלי לגעת ברשת האמיתית | Playwright `page.route()` |
| **E2E** | תרחישי משתמש מלאים על הקוד האמיתי, בדפדפן אמיתי, עם נתונים מוזרקים | Playwright + שרת פיתוח מקומי |
| **Regression** | כל באג אמיתי שכבר נתפס בעבר - נשאר כבדיקה קבועה לצמיתות | כל אחת מהרמות לעיל, לפי אופי הבאג |

### רמות אמינות (Confidence Tiers)

- 🟢 **מאומת במלואו** - הרמה מוכיחה בוודאות שההתנהגות נכונה
- 🟡 **מאומת חלקית** - מוכיח את הצד המקומי/החוזה (contract), לא את ההתנהגות האמיתית של Google Apps Script/Sheets/Drive עצמם
- 🔴 **לא ניתן לאימות אוטומטי** - דורש מכשיר אמיתי (iOS/Android) או בדיקה ידנית חד-פעמית

### רמות חומרה (Severity, לתיעדוף ממצאים)

| חומרה | הגדרה | דוגמה מהפרויקט |
|---|---|---|
| **Critical** | אובדן נתונים, או פעולה שנראית כאילו הצליחה אך לא | ה-crash של Apps Script על גיליון ריק (כל שמירה נכשלה בשקט) |
| **High** | פיצ'ר ליבה לא עובד במכשיר/דפדפן נפוץ | ה-capture bug ב-iOS (לא ניתן בכלל להעלות תמונה) |
| **Medium** | פיצ'ר עובד חלקית / חוסר עקביות | addConfigOption שלא עשה כלום בפועל |
| **Low** | קוסמטי, לא פוגע בתפקוד | יישור/מרווחים |

### סביבת הבדיקה

- שרת הפיתוח המקומי (`npm run dev`) מריץ **את אותו קוד קליינט בדיוק** שרץ
  ב-GitHub Pages.
- לבדיקות שדורשות "הצלחה מלאה" מול ה-Apps Script (ר' Integration/Contract
  לעיל) - מדמים תשובה מוצלחת דרך `page.route()`, כדי לבדוק את ההתנהגות
  של האפליקציה בלי תלות ברשת אמיתית.
- לבדיקות Service Worker/PWA - **חובה** לרוץ מול `vite build && vite
  preview`, לא מול שרת הפיתוח (ה-SW לא פעיל אותו דבר במצב dev).
- **מגבלה קבועה**: הסביבה שבה הבדיקות רצות חסומה ברמת מדיניות ארגונית
  מגישה לאינטרנט הרגיל (script.google.com, github.io וכו'). זו לא מגבלה
  טכנית לפתרון - זו מדיניות אבטחה מכוונת. כל מה שדורש רשת אמיתית נשאר 🟡/🔴.

---

## 2. מטריצת מעקב דרישות (Requirements Traceability Matrix)

כל שורה = דרישה אחת מ-SPEC.md. עמודת "סטטוס מימוש" נבדקה ישירות מול הקוד
הקיים (לא הנחה).

### מבנה הנתונים

| # | דרישה (SPEC.md) | סטטוס מימוש | בדיקה | אמינות |
|---|---|---|---|---|
| DM-01 | `row_id` תמיד קיים, נוצר בלקוח, מפתח סנכרון יחיד | ✅ תואם | TC-DM-001 | 🟢 |
| DM-02 | `serial_number` אופציונלי, לא משפיע על סנכרון | ✅ תואם | TC-DM-002 | 🟢 |
| DM-03 | `sku` יכול לחזור על עצמו בכמה שורות | ✅ תואם | TC-DM-003 | 🟢 |
| DM-04 | `type`/`location`/`physical_status` - רשימות ניתנות להרחבה, לא קבועות בקוד | ✅ תואם | TC-CFG-001 | 🟢 |
| DM-05 | `availability_status` - ברירת מחדל "זמין", "נמכר" נשאר גלוי וניתן לסינון | ✅ תואם | TC-CRUD-004 | 🟢 |
| DM-06 | `price` אופציונלי, ניתן להשלמה בדיעבד | ✅ תואם | TC-CRUD-005 | 🟢 |
| DM-07 | `notes` - טקסט חופשי בלבד, ללא לוגיקה נוספת | ✅ תואם | TC-DM-004 | 🟢 |
| DM-08 | `is_deleted` שונה מ-`availability_status`, מוסתר לחלוטין לתמיד | ✅ תואם | TC-CRUD-006 | 🟢 |
| DM-09 | `last_modified_by/at` נשמרים ומוצגים | ✅ תואם | TC-DM-005 | 🟢 |
| DM-10 | `last_modified_at` נקבע **בצד השרת**, לא שעון הלקוח | ⚠️ חלקי - הלקוח כן קובע ערך זמני עד לסנכרון מוצלח | TC-SYNC-006 | 🟡 |

### טאב Config

| # | דרישה | סטטוס מימוש | בדיקה | אמינות |
|---|---|---|---|---|
| CFG-01 | רשימות ניתנות להרחבה, הוספה בלבד (לא מחיקה) | ✅ תואם | TC-CFG-001 | 🟢 |
| CFG-02 | הוספה inline מטופס פריט ("+") | ✅ תואם | TC-CFG-001 | 🟢 |
| CFG-03 | נכתב חזרה ל-Config דרך Apps Script, זמין לכולם ברענון הבא | ✅ תואם (אחרי התיקון ל-addConfigOption) | TC-CFG-002 | 🟡 |
| CFG-04 | מסך ניהול רשימות ייעודי (שלב 2) | ❌ **לא נבנה** | - | 🔴 (לא רלוונטי - אין מה לבדוק) |
| CFG-05 | ערכי ברירת מחדל תואמים לאקסל | ✅ תואם | TC-CFG-003 | 🟢 |

### חיפוש וסינון

| # | דרישה | סטטוס מימוש | בדיקה | אמינות |
|---|---|---|---|---|
| SRCH-01 | חיפוש לפי מק"ט - טקסטואלי, כמה תוצאות | ✅ תואם | TC-SRCH-001 | 🟢 |
| SRCH-02 | חיפוש לפי מספר סידורי - **מדויק**, פריט בודד | ❌ **לא תואם** - כרגע גם זה חיפוש חלקי (`.includes()`) | TC-SRCH-002 (בדיקה שכרגע **תיכשל** במכוון, עד לתיקון) | 🟢 |
| SRCH-03 | סינון "ללא מחיר" | ✅ תואם | TC-SRCH-003 | 🟢 |
| SRCH-04 | סינון "ללא מק"ט" | ✅ תואם | TC-SRCH-004 | 🟢 |
| SRCH-05 | סינון לפי availability_status (זמין/הכל/נמכר) | ✅ תואם | TC-SRCH-005 | 🟢 |
| SRCH-06 | שילוב כמה פילטרים - חיתוך (AND), לא איחוד | ✅ תואם | TC-SRCH-006 | 🟢 |

### ניהול פריטים (CRUD)

| # | דרישה | סטטוס מימוש | בדיקה | אמינות |
|---|---|---|---|---|
| CRUD-01 | הוספה - `serial_number`/`price` יכולים להישאר ריקים | ✅ תואם | TC-CRUD-001 | 🟢 |
| CRUD-02 | עריכה - כולל השלמת שדות חסרים | ✅ תואם | TC-CRUD-002 | 🟢 |
| CRUD-03 | שינוי ל"נמכר" - עריכה רגילה, **ללא** חלון אישור | ✅ תואם | TC-CRUD-004 | 🟢 |
| CRUD-04 | מחיקה - **עם** חלון אישור, soft-delete | ✅ תואם | TC-CRUD-006/007 | 🟢 |
| CRUD-05 | כל המשתמשים ברמת הרשאה שווה | ✅ תואם (אין בכלל מנגנון הרשאות) | - | 🟢 (טריוויאלי) |

### תמונות

| # | דרישה | סטטוס מימוש | בדיקה | אמינות |
|---|---|---|---|---|
| IMG-01 | העלאה ישירה ממצלמה/גלריה של המכשיר | ✅ תואם בקוד, אך שני באגים אמיתיים נתפסו כבר (iOS capture, Android) | TC-MOBILE-004 | 🔴 (התנהגות מצלמה אמיתית) |
| IMG-02 | כיווץ בצד לקוח לפני העלאה (JPEG, רוחב סביר) | ✅ תואם - 900px, לא 1600px כמו הדוגמה ב-SPEC (לא סתירה, רק דוגמה שונה) | TC-IMG-001 | 🟢 |
| IMG-03 | base64 → Apps Script → Drive → `image_url` מתעדכן | ✅ תואם בתור המקומי (אחרי התיקון) | TC-IMG-002 | 🟡 |
| IMG-04 | אופליין - תמונה ממתינה, מועלית כשחוזר החיבור | ✅ תואם | TC-SYNC-003 | 🟢 |

### סנכרון

| # | דרישה | סטטוס מימוש | בדיקה | אמינות |
|---|---|---|---|---|
| SYNC-01 | Sheets כמקור אמת מרכזי מאחורי Apps Script | ✅ תואם (ארכיטקטורה) | - | 🟡 |
| SYNC-02 | רענון אוטומטי כל 30-60 שניות + כפתור ידני | ✅ תואם (45 שניות) | TC-SYNC-001 | 🟢 |
| SYNC-03 | סדר: קודם דוחפים שינויים, אח"כ מושכים סנפשוט | ✅ תואם | TC-SYNC-002 | 🟢 |
| SYNC-04 | כישלון - ממשיך לנסות, אחרי סף (5) כשלונות → סימון גלוי | ✅ תואם | TC-SYNC-004 | 🟢 |
| SYNC-05 | כישלון pull → ממשיך להציג נתונים מקומיים + **"לא עודכן מאז HH:MM"** | ❌ **לא נבנה** - `lastSyncedAt` מחושב אך לעולם לא מוצג | TC-SYNC-005 (תיכשל במכוון עד לתיקון) | 🟢 |

### עבודה אופליין

| # | דרישה | סטטוס מימוש | בדיקה | אמינות |
|---|---|---|---|---|
| OFF-01 | עותק מקומי מלא (IndexedDB) | ✅ תואם | TC-OFF-001 | 🟢 |
| OFF-02 | אופליין: צפייה/חיפוש/הוספה/עריכה - הכל עובד | ✅ תואם | TC-OFF-002 | 🟢 |
| OFF-03 | חיבור חוזר → סנכרון אוטומטי | ✅ תואם | TC-OFF-003 | 🟢 |

### פתרון התנגשויות

| # | דרישה | סטטוס מימוש | בדיקה | אמינות |
|---|---|---|---|---|
| CONF-01 | Last-Write-Wins לפי timestamp **שרת** | ✅ תואם בקוד (Code.gs קובע), נבדק רק דרך חוזה מדומה | TC-SYNC-006 | 🟡 |
| CONF-02 | אין מיזוג, אין התראה על דריסה (החלטה מכוונת) | ✅ תואם (עיצוב, לא באג) | - | 🟢 (טריוויאלי) |

### רב-לשוניות

| # | דרישה | סטטוס מימוש | בדיקה | אמינות |
|---|---|---|---|---|
| I18N-01 | עברית/אנגלית/הולנדית, כפתור בכותרת | ✅ תואם | TC-I18N-001/002/003 | 🟢 |
| I18N-02 | רק תוויות UI מתורגמות; תוכן (כולל ערכי Config) לא; חריג: availability_status כן מתורגם | ✅ תואם | TC-I18N-004 | 🟢 |
| I18N-03 | כיוון RTL קבוע תמיד, גם באנגלית/הולנדית | ✅ תואם | TC-I18N-005 | 🟢 |
| I18N-04 | מחיר תמיד ב-₪ | ✅ תואם | TC-I18N-006 | 🟢 |
| I18N-05 | בחירת שפה נשמרת per-device | ✅ תואם | TC-I18N-007 | 🟢 |
| I18N-06 | רשימת שמות הזהות תמיד בעברית, בלי קשר לשפה | ✅ תואם (בבדיקה ישירה - אין להם מפתח תרגום כלל) | TC-I18N-008 | 🟢 |
| I18N-07 | כל מפתח `t()` בשימוש קיים בשלוש השפות (אין מחרוזת חסרה) | לא נבדק אף פעם עד היום | TC-I18N-009 | 🟢 (בדיקה סטטית, "יבשה") |

### Backend API - לוגיקת Apps Script

| # | דרישה | סטטוס מימוש | בדיקה | אמינות |
|---|---|---|---|---|
| API-01 | `getAll` מחזיר רק שורות פעילות + Config | ✅ תואם | TC-BE-001 | 🟢 (unit, לוגיקה מבודדת) |
| API-02 | `upsert` יוצר/מעדכן לפי `row_id`, קובע timestamp בשרת | ✅ תואם | TC-BE-002 | 🟢 |
| API-03 | `softDelete` מסמן `is_deleted` לפי `row_id` | ✅ תואם | TC-BE-003 | 🟢 |
| API-04 | `uploadImage` - שומר ל-Drive, מחזיר URL תקין (thumbnail format, אחרי התיקון) | ✅ תואם | TC-BE-004 | 🟢 |
| API-05 | `addConfigOption` - בודק כפילות case-insensitive | ✅ תואם | TC-BE-005 | 🟢 |
| TECH-01 | גוף בקשה כ-`text/plain`, לא `application/json` (עוקף CORS preflight) | ✅ תואם | TC-BE-006 | 🟢 |
| TECH-02 | `LockService.getScriptLock()` על כל endpoint שכותב | ✅ תואם (בקוד, נבדק בעין - Apps Script לא ניתן להרצה מקומית) | - | 🔴 |
| TECH-03 | סוד משותף נבדק בכל בקשה | ✅ תואם | TC-BE-007 | 🟢 |

---

## 3. קטלוג תרחישי הבדיקה

מבנה כל תרחיש: מזהה, חומרה, תנאי קדם, צעדים, תוצאה צפויה.

### Unit - לוגיקת Apps Script (TC-BE-*)

> חדש: מחלצים את הפונקציות הטהורות מ-`Code.gs` (מציאת שורה לפי `row_id`,
> בניית קישור Drive, השוואת ערכי Config) לקובץ JS רגיל הניתן להרצה תחת
> Node - כדי לבדוק את הלוגיקה בלי Google בכלל. **שני הבאגים האמיתיים
> שכבר תפסנו ב-Code.gs (קריסה על גיליון ריק, קריסה על תא >50,000 תווים)
> היו נתפסים מיד ברמה הזו.**

| מזהה | חומרה | תרחיש | תוצאה צפויה |
|---|---|---|---|
| TC-BE-001 | High | `getAll` על גיליון עם שורות `is_deleted=true` ו-`false` מעורבות | מחזיר רק את הפעילות |
| TC-BE-002 | Critical | `upsert` על `row_id` קיים מול חדש | מעדכן שורה קיימת בלי כפילות; שורה חדשה נוספת בסוף |
| TC-BE-003 | High | `softDelete` על `row_id` שלא קיים | מחזיר שגיאה מבוקרת, לא קורס |
| TC-BE-004 | High | `uploadImage` - בניית הקישור מתוך מזהה קובץ | הקישור בפורמט `thumbnail?id=...&sz=w1000`, לא `uc?export=view` |
| TC-BE-005 | Medium | `addConfigOption` עם ערך קיים באות גדולה/קטנה שונה | לא נוסף בכפילות |
| TC-BE-006 | Critical | **רגרסיה**: `findRowIndexByRowId` על גיליון עם שורת כותרת בלבד (0 שורות נתונים) | לא זורק שגיאה, מחזיר -1 |
| TC-BE-007 | Critical | **רגרסיה**: כתיבת עמודת `image_url` כשיש בה תא קיים עם מחרוזת >50,000 תווים | לא זורק שגיאה, כותב רק לתאים שבאמת משתנים |
| TC-BE-008 | High | בקשה בלי סוד משותף / עם סוד שגוי | מוחזרת שגיאת `forbidden`, לא מבוצעת שום פעולה |

### Integration/Contract - מול Apps Script מדומה (TC-SYNC-*, page.route)

| מזהה | חומרה | תרחיש | תוצאה צפויה |
|---|---|---|---|
| TC-SYNC-001 | Medium | טיימר הרענון האוטומטי | קריאת סנכרון מתבצעת כל ~45 שניות |
| TC-SYNC-002 | Critical | סדר קריאות בסנכרון (מדומה) | `upsert`/`uploadImage` נשלחים **לפני** `getAll` |
| TC-SYNC-003 | High | הוספת פריט עם תמונה, אופליין | נוצרות שתי פעולות בתור: `upsert` ואז `uploadImage` |
| TC-SYNC-004 | Critical | תשובת שרת מדומה שנכשלת 5 פעמים רצוף על אותו שינוי | הצ'יפ "ממתינים" הופך לאדום (syncProblem) |
| TC-SYNC-005 | High | תשובת `getAll` מדומה שמחזירה שגיאה | (כרגע ייכשל - אין הודעת "לא עודכן מאז" בכלל) |
| TC-SYNC-006 | Critical | תשובת `upsert` מדומה מצליחה, מחזירה `last_modified_at` אחר מזה של הלקוח | הרשומה המקומית מתעדכנת לזמן שהשרת החזיר, לא נשארת עם ניחוש הלקוח |
| TC-SYNC-007 | Medium | תשובת `uploadImage` מדומה מצליחה עם URL חדש | `image_url` המקומי מתעדכן לקישור האמיתי שהוחזר |

### E2E - ניהול פריטים (TC-CRUD-*, TC-DM-*)

| מזהה | חומרה | תרחיש | תוצאה צפויה |
|---|---|---|---|
| TC-CRUD-001 | Critical | הוספת פריט - שם בלבד, שאר השדות ריקים | נשמר בהצלחה, מופיע ברשימה |
| TC-CRUD-002 | High | עריכת פריט קיים - השלמת מחיר/מס' סידורי שהיו ריקים | הערכים החדשים נשמרים ומוצגים |
| TC-CRUD-003 | High | הוספה עם שם ריק | שגיאת ולידציה, לא נשמר, לא נכנס לתור |
| TC-CRUD-004 | High | סימון פריט כ"נמכר" | נשמר **בלי** חלון אישור; סרט "נמכר" מופיע; ניתן לסינון |
| TC-CRUD-005 | Medium | פריט ללא מחיר | לא מוצג שדה מחיר בכרטיס כלל |
| TC-CRUD-006 | Critical | מחיקת פריט - אישור | נעלם מהרשימה לצמיתות, `is_deleted=true` |
| TC-CRUD-007 | High | מחיקת פריט - ביטול בחלון האישור | נשאר ללא כל שינוי |
| TC-CRUD-008 | Medium | ביטול טופס הוספה/עריכה בלי שמירה | אפס כתיבות ל-DB המקומי |
| TC-CRUD-009 | Low | שם/הערה ארוכים במיוחד (200+ תווים) | לא שובר את פריסת הכרטיס |
| TC-DM-001-005 | Medium | שדות `row_id`/`serial_number`/`sku`/`notes`/`last_modified_*` בטופס ובכרטיס | תואמים בדיוק למבנה הנתונים ב-SPEC |

### E2E - חיפוש וסינון (TC-SRCH-*)

| מזהה | חומרה | תרחיש | תוצאה צפויה |
|---|---|---|---|
| TC-SRCH-001 | High | חיפוש מק"ט חלקי ("302") | מחזיר את כל הפריטים המתאימים |
| TC-SRCH-002 | Medium | חיפוש מספר סידורי חלקי | **צפוי להיכשל כרגע** (ר' פער SRCH-02 לעיל) - עד לתיקון |
| TC-SRCH-003/004 | Medium | פילטרי "ללא מחיר"/"ללא מק"ט" | מציגים רק פריטים עם השדה ריק |
| TC-SRCH-005 | High | פילטר זמינות (זמין/הכל/נמכר) | תוצאה מדויקת לכל מצב |
| TC-SRCH-006 | Medium | 2+ פילטרים במקביל | חיתוך (AND) נכון |
| TC-SRCH-007 | Low | הוספת פריט חדש שלא תואם לפילטר הפעיל | **ממצא UX קיים** - הפריט נשמר אך "נעלם" בלי הודעה. הבדיקה מתעדת את ההתנהגות הנוכחית (לא בהכרח כישלון - החלטת מוצר) |

### E2E - רשימות דינמיות (TC-CFG-*)

| מזהה | חומרה | תרחיש | תוצאה צפויה |
|---|---|---|---|
| TC-CFG-001 | High | הוספת ערך חדש ל-type/location/status דרך "+" | מופיע מיד ברשימה, ניתן לבחירה ולשמירה |
| TC-CFG-002 | Medium | אותו ערך באות גדולה/קטנה שונה | לא נוסף פעמיים |
| TC-CFG-003 | Low | ערכי ברירת המחדל בעת טעינה ראשונה | תואמים בדיוק לרשימה ב-SPEC |

### E2E - סנכרון/אופליין אמיתי (TC-OFF-*)

| מזהה | חומרה | תרחיש | תוצאה צפויה |
|---|---|---|---|
| TC-OFF-001 | High | ניתוק (Playwright offline) | נקודת סטטוס אדומה, "מנותק" |
| TC-OFF-002 | Critical | הוספה/עריכה באופליין | הפעולה מצליחה מקומית, "ממתין לסנכרון" עם מספר נכון |
| TC-OFF-003 | High | חיבור מחדש | ניסיון סנכרון אוטומטי מתבצע (נצפה דרך spy על הקריאה) |

### E2E - שפה/RTL (TC-I18N-*)

| מזהה | חומרה | תרחיש | תוצאה צפויה |
|---|---|---|---|
| TC-I18N-001/002/003 | Medium | מעבר לכל שפה | כל מחרוזות ה-UI מתעדכנות |
| TC-I18N-004 | High | ערך Config (למשל "אביגדור") באנגלית | נשאר בעברית, לא מתורגם |
| TC-I18N-005 | High | `dir` של הדף בכל שפה | תמיד `rtl` |
| TC-I18N-006 | Low | תצוגת מחיר בכל שפה | תמיד עם סימן ₪ |
| TC-I18N-007 | Medium | רענון דף אחרי בחירת שפה | הבחירה נשמרת |
| TC-I18N-008 | Medium | מסך בחירת זהות באנגלית פעילה | שמות הצוות מוצגים בעברית |
| TC-I18N-009 | High | כל מפתחות ה-`t()` בקוד מול 3 טבלאות התרגום | אין אף מפתח חסר באף שפה |

### E2E - מובייל (TC-MOBILE-*)

| מזהה | חומרה | תרחיש | תוצאה צפויה |
|---|---|---|---|
| TC-MOBILE-001 | Critical | **רגרסיה**: iPhone 13 - פריסת הטולבר | תיבת החיפוש בשורה נפרדת, לא נדחסת |
| TC-MOBILE-002 | Medium | מסך אנדרואיד נפוץ (Pixel) - אותה בדיקה | פריסה תקינה |
| TC-MOBILE-003 | Medium | מודל מלא-מסך במובייל | ממלא את כל הגובה/רוחב |
| TC-MOBILE-004 | 🔴 | בורר תמונה אמיתי (מצלמה/גלריה) ב-iOS וב-Android | **לא ניתן לאוטומציה** - נשאר תמיד ידני |

### PWA / Service Worker (TC-PWA-*)

| מזהה | חומרה | תרחיש | תוצאה צפויה |
|---|---|---|---|
| TC-PWA-001 | Medium | build אמיתי (`vite build && vite preview`) + ניתוק רשת | האפליקציה עדיין נטענת (קאשינג נכסים) |
| TC-PWA-002 | 🔴 | התקנה בפועל למסך הבית, מצב standalone | דורש מכשיר אמיתי |

---

## 4. חבילת רגרסיה - כל באג אמיתי שכבר נתפס

| מזהה | תיאור הבאג המקורי | היכן נתפס | בדיקה קבועה |
|---|---|---|---|
| REG-001 | טולבר נשבר במובייל (search box נדחס לשורה עם הפילטרים) | QA ידני, מובייל | TC-MOBILE-001 |
| REG-002 | קריסת Apps Script על גיליון ריק (`findRowIndexByRowId`) | דיווח משתמש בפועל | TC-BE-006 |
| REG-003 | קריסת Apps Script על תא >50,000 תווים ב-`fixImageUrls` | דיווח משתמש בפועל | TC-BE-007 |
| REG-004 | `image_url` בפורמט לא-אמין (`uc?export=view`) - תמונות לא מוצגות | דיווח משתמש בפועל | TC-BE-004 |
| REG-005 | `addConfigOption` היה no-op - ערכים חדשים לא הגיעו לגיליון | נמצא בסקירת קוד | TC-CFG-001 (מול חוזה מדומה) |
| REG-006 | תמונה חדשה נשלחה כ-base64 גולמי בעמודת `image_url` (סיכון לחריגת 50K) | נמצא בסקירת קוד | TC-SYNC-003 |
| REG-007 | תמונת קטלוג מוחלפת בטעות בפריט ייחודי | דרישת בעל העסק | (חד-פעמי, לא רלוונטי לרגרסיה שוטפת) |
| REG-008 | שדה תמונה עם `capture="environment"` לא נפתח ב-iOS standalone | דיווח משתמש בפועל | TC-MOBILE-004 (מתועד כמגבלה קבועה, לא ניתן לרגרסיה אוטומטית) |

---

## 5. מה שתמיד יישאר ידני (רשימה סגורה)

1. **התנהגות מצלמה/גלריה אמיתית** ב-iOS וב-Android (TC-MOBILE-004)
2. **התקנה בפועל למסך הבית** ומצב standalone אמיתי (TC-PWA-002)
3. **הצלחה אמיתית מול הגיליון/Drive האמיתיים** (רק "מדומה" מאומת אוטומטית)
4. **`LockService` תחת עומס מקבילי אמיתי** (TECH-02) - Apps Script לא ניתן להרצה מקומית

---

## 6. פורמט הדיווח למשתמש

בסיום כל הרצה - דוח קצר בעברית פשוטה, לא ז'רגון טכני:

```
✅ 34 תרחישים עברו
❌ 2 נכשלו:
   - חיפוש מספר סידורי מחזיר גם התאמות חלקיות (SRCH-02, פער ידוע)
   - אין הודעת "לא עודכן מאז" כשסנכרון נכשל (SYNC-05, פער ידוע)
⏸️ 2 נשארים לבדיקה ידנית שלך: מצלמה במכשיר אמיתי, התקנה למסך הבית
```

---

## נספח: פערים פתוחים

ר' משימה #15 ברשימת המשימות - כל הפערים שנמצאו כאן (SRCH-02, SYNC-05,
CFG-04, ותיעוד המיגרציה ב-SPEC.md) מרוכזים שם לטיפול עתידי, יחד עם כל
ממצא נוסף שהבדיקות עצמן יעלו.
